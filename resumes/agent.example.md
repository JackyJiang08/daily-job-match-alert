# AI Agent resume placeholder

Replace this file with the complete plain-text or Markdown content of your AI Agent resume and save it as `agent.md` (the file name is the track id from `resumes.tracks` in `config.json`).

Do not put API keys, account passwords, Social Security numbers, or other secrets in this file.
